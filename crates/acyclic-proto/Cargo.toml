[package]
name = "acyclic-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false
description = "CLI <-> daemon wire types: newline-delimited JSON over the store's unix socket"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
